import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "MinneTherapy - Connect with Occupational Therapists in Minnesota",
  description: "Find and connect with qualified occupational therapists in your area. MinneTherapy makes it easy to schedule appointments and manage your therapy journey.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
